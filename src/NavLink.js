import React from 'react';
import { Link } from 'react-router-dom'
import cx from 'classnames';

function NavLink(props){
  return "";
  // let link = props.link;
  // let base = "#";
  //     return(
  //     <li className={cx("nav-item", { 'active': location.hash === `${base}${link.href}` })}>
  //       <Link className={cx("nav-link", { 'active': location.hash === `${base}${link.href}` })} to={link.href}> {link.title}</Link>
  //     </li>);
}
export default NavLink;
